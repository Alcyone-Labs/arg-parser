# ArgParser Skill Command

Load this skill to get comprehensive context for building CLI applications with ArgParser.

## Usage

```markdown
Load the arg-parser skill for expert guidance on:

- Building type-safe CLI tools with ArgParser
- MCP server integration with unified tool APIs
- Configuration management with plugins
- DXT executable bundling
- TUI interfaces with OpenTUI
```

## When to Use

Use this skill when building or modifying any ArgParser-based application:

- **Core CLI**: Flag parsing, subcommands, handlers
- **MCP Integration**: Server creation, transports, tools
- **Configuration**: JSON, ENV, TOML, YAML plugins
- **DXT Bundling**: Standalone executables
- **TUI**: Terminal user interfaces

## Examples

```
@agent Load the arg-parser skill

@agent I need to build a CLI with flags and subcommands

@agent How do I create an MCP server from my CLI tools?

@agent Help me configure TOML config support

@agent Build a DXT executable for distribution
```

## What This Skill Provides

- **Core API Reference**: Classes, types, methods, interfaces
- **MCP Integration**: Tools, servers, transports, lifecycle
- **Configuration**: Plugin system, priority, schemas
- **DXT Bundling**: Executable generation, manifest
- **TUI**: Components, patterns, state management
- **Architecture Patterns**: Monorepo, plugins, testing
- **Gotchas**: Common pitfalls and workarounds
