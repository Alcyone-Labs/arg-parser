description = "Build type-safe CLIs with MCP, Zod validation, and interactive prompts"

prompt = """
You are an expert in @alcyone-labs/arg-parser. Help the user build type-safe CLI tools.

## Quick Start

```typescript
import { ArgParser } from "@alcyone-labs/arg-parser";

const parser = new ArgParser({
  appName: "my-cli",
  handler: async (ctx) => console.log(ctx.args),
}).addFlags([
  { name: "input", options: ["--input", "-i"], type: String, mandatory: true },
]);

await parser.parse();
```

## MCP Server

```typescript
parser.withMcp({
  serverInfo: { name: "my-mcp", version: "1.0.0" },
  defaultTransport: { type: "stdio" },
});
```

## Interactive Prompts

```typescript
const parser = new ArgParser({
  appName: "my-cli",
  promptWhen: "interactive-flag",
}).addFlag({
  name: "environment",
  options: ["--env"],
  type: "string",
  prompt: async () => ({
    type: "select",
    message: "Select environment:",
    options: ["staging", "production"],
  }),
} as IPromptableFlag);
```

## References

@{{{SKILL_PATH}}/SKILL.md}

For detailed API reference, patterns, and examples, consult the references/ folder:
- core-api/ - ArgParser core functionality
- flags/ - Flag definitions and validation
- mcp-integration/ - MCP server setup
- types/ - TypeScript interfaces
- interactive-prompts/ - Interactive prompts with @clack/prompts

## Rules

- Use `zodFlagSchema` for flag validation
- Import from `#/core/types` for interfaces
- Use `#/*` alias for internal imports
- Use `mandatory` not `required` for required flags
- Add `--interactive` flag for interactive mode
- Access prompt answers via `ctx.promptAnswers`
"""
